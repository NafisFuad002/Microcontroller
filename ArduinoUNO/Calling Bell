// C++ code
//
void setup()
{
  pinMode(2, INPUT);
  pinMode(4, OUTPUT);
  //pinMode(LED_BUILTIN, OUTPUT);
}

void loop()
{
  if(digitalRead(2)==HIGH)
  {
    digitalWrite(4, HIGH);
  }
  else
  {
    digitalWrite(4, LOW);
  }
}
