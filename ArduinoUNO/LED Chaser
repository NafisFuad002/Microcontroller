// C++ code
//
void setup()
{
  int port_numbers[5]={2,3,4,5,6};
  for(int i=0; i<5; i++)
  {
    pinMode(port_numbers[i], OUTPUT);
  }
}

void loop()
{
  int port_numbers[5]={2,3,4,5,6};
  for(int i=0; i<5; i++)
  {
    digitalWrite(port_numbers[i], HIGH);
    delay(500);
    digitalWrite(port_numbers[i], LOW);
  }
  for(int i=3; i>=1; i--)
  {
    digitalWrite(port_numbers[i], HIGH);
    delay(500);
    digitalWrite(port_numbers[i], LOW);
  }
}
